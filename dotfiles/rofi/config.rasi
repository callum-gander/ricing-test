/* ~/.config/rofi/config.rasi — Catppuccin Mocha (high-contrast) */

configuration {
    modi: "drun,run,window";
    show-icons: true;
    drun-display-format: "{name}";
    font: "JetBrainsMono Nerd Font 12";
}

* {
    bg:      #1e1e2e;
    bg-alt:  #313244;
    fg:      #cdd6f4;
    accent:  #89b4fa;
    urgent:  #f38ba8;
    muted:   #9399b2;
    background-color: transparent;
    text-color: @fg;
}

window {
    background-color: @bg;
    border: 2px;
    border-color: @accent;
    border-radius: 16px;
    width: 640px;
    padding: 14px;
}

mainbox {
    children: [ inputbar, message, listview ];
    spacing: 12px;
    background-color: transparent;
}

inputbar {
    background-color: @bg-alt;
    text-color: @fg;
    border-radius: 12px;
    padding: 10px 14px;
    spacing: 10px;
    children: [ prompt, entry ];
}
prompt { text-color: @accent; background-color: transparent; }
entry  { text-color: @fg; background-color: transparent; placeholder: "Search apps..."; placeholder-color: @muted; }

/* error / status messages (this is what was unreadable before) */
message {
    background-color: @bg-alt;
    text-color: @fg;
    border-radius: 12px;
    padding: 10px 14px;
}
textbox { text-color: @fg; background-color: transparent; }

listview {
    lines: 8;
    columns: 1;
    spacing: 4px;
    scrollbar: false;
    background-color: transparent;
}

element {
    padding: 8px 12px;
    border-radius: 10px;
    spacing: 10px;
    background-color: transparent;
    text-color: @fg;
}
element normal normal    { text-color: @fg; }
element alternate normal { text-color: @fg; }
element selected normal   { background-color: @accent; text-color: @bg; }
element-icon { size: 24px; background-color: transparent; }
element-text { text-color: inherit; background-color: transparent; vertical-align: 0.5; }
